# KSHETRA frontend environment
# Copy to `.env` (or `.env.local`) to override.

# Base URL of the FastAPI prediction service.
# Default when unset: http://127.0.0.1:8000
VITE_API_BASE_URL=http://127.0.0.1:8000
