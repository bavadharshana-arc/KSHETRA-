# KSHETRA FastAPI Backend Dependencies
fastapi>=0.110.0
uvicorn>=0.28.0
pydantic>=2.6.0
lightgbm>=4.3.0
scikit-learn>=1.4.0
pandas>=2.1.0,<3.0.0
numpy>=1.26.0
joblib>=1.3.0
shap>=0.45.0
lifelines>=0.29.0

# Persistence layer (Step 1 of the persistence plan) — SQLite via SQLAlchemy,
# schema managed by Alembic migrations. Not used by /, /health, or /predict.
sqlalchemy>=2.0.0
alembic>=1.13.0
